<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Display Latest Image</title>
</head>
<body>
  <h1>Latest Image</h1>
  
  <% if (latestFace) { %>
    <!-- Hiển thị ảnh từ URL -->
    <img src="<%= latestFace %>" alt="Latest Road Image" style="max-width: 100%; height: auto;">
  <% } else { %>
    <p>No image available.</p>
  <% } %>
</body>
</html>
